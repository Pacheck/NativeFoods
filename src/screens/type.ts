import { NavigationStackProp } from "react-navigation-stack";

export interface IDetailNavProp {
    navigation: NavigationStackProp
}

export interface IResult {
    name: string;
    photos: string[];
};